# GymMate

헬스 루틴을 만들고 세트마다 체크하는 웹앱입니다.

| 주소 | 내용 |
| --- | --- |
| `/test/` | 안내 페이지 (짐메이트로 들어가는 입구) |
| `/test/gymmate/` | **짐메이트** — 앱 본체 · [문서](gymmate/README.md) |

```bash
npx http-server . -p 8080 --silent
# → http://localhost:8080/          안내 페이지
# → http://localhost:8080/gymmate/  짐메이트
```

## 왜 앱이 폴더 안에 있나

설치형 웹앱(PWA)은 자기 **영역(scope)** 을 매니페스트가 놓인 위치 기준으로 잡습니다.
짐메이트가 루트에 있으면 영역이 `/test/` 전체가 되어, 같은 저장소에 다른 앱을 두었을 때
그 앱이 짐메이트의 한 페이지로 취급되는 문제가 있었습니다. 그래서 앱을 폴더 안에
두어 영역을 명확히 합니다.

매니페스트의 `id` 는 적지 않습니다. 적지 않으면 `start_url` 이 그대로 앱의 신원이
되어 실제 주소와 항상 일치합니다.

> 짐메이트 주소가 `/test/` 에서 `/test/gymmate/` 로 바뀐 적이 있습니다.
> 예전 아이콘은 안내 페이지로 열리며, 거기서 다시 설치하면 됩니다. 기록은 그대로 남습니다.
> 루트의 `sw.js` 는 예전 주소에 남아 있던 서비스워커를 스스로 걷어내는 역할만 합니다.

## `majungmul/` 은 무엇인가

물 마시기 기록 앱 **마중물**이 있던 자리입니다. 앱은 폐기했지만, 홈 화면에
설치한 사람의 기기에는 서비스워커와 캐시가 남습니다. 그대로 두면 앱이 사라진
뒤에도 낡은 화면이 계속 뜨므로, 그 자리에 두 개의 파일만 남겨 두었습니다.

- `index.html` — 종료를 알리고, 기기에 남은 기록을 내려받을 기회를 줍니다
- `sw.js` — 남아 있는 서비스워커와 캐시를 스스로 걷어냅니다

물 마신 기록(localStorage)은 지우지 않습니다. 내려받기 전에 지워 버리면
되돌릴 수 없기 때문입니다. 사용자가 앱을 지우면 함께 사라집니다.

## 배포

`.github/workflows/pages.yml` 이 저장소 전체를 GitHub Pages 로 올립니다.
